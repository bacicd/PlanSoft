@*
INCLUDE YOUR IDENTITY CHECK HERE!
@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
            
       <li class="dropdown">
           <a href="#" role="button" class="dropdown-toggle" data-hover="dropdown"> <i class="glyphicon glyphicon-user"></i> 
               @User.Identity.GetUserName()
            
            <i class="caret"></i></a>
           <ul class="dropdown-menu">
               <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
               <li>@Html.ActionLink("Manage my account", "Manage", "Account", routeValues: null, htmlAttributes: new { title = "Manage" })</li>
               <li><a href="#">Something else here</a></li>
               <li role="presentation" class="divider"></li>
           </ul>
       </li>
}
else
{
*@
@*odel IEnumerable<MvcAdminTemplate.Models.Account>*@
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
<script src="//code.jquery.com/jquery-2.1.1.min.js"></script>
<script>
    function populateName() {
        $.ajax({
            cache: false,
            type: "GET",
            url: '@Url.Action("firstNameLastName", "Home")',
            dataType: 'json',
            success: function (data) {
                console.log(data.fname, data.lname);
                $("#fname").html(data.fname);
                $("#lname").html(data.lname);
            },
            error: function (xhr, ajaxOptions, thrownError) {
            }
        });
    }
    populateName();
</script>
<style>
    
        #menuitems a:hover { border-bottom:3px solid #808080;}
</style>
@if (Request.IsAuthenticated)
{
    <li id="menuitems">@Html.ActionLink("Log Out", "Logout", "Account")</li>
    <li id="menuitems" style="margin-top:1.4%"><strong>@*@Html.Encode(User.Identity.Name)*@<label id="fname"></label>&nbsp;<label id="lname"></label></strong></li>
}
else
{
    @*<li id="menuitems">@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>*@
    <li id="menuitems">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
}
@*}*@
